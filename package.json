{
  "name": "elements",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "npm cache clean && karma start --single-run"
  },
 "devDependencies": {
   "jasmine-core": "^2.5.2",
   "karma": "^1.3.0",
   "karma-chrome-launcher": "^2.0.0",
    }
}
