{
  "name": "elements",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "karma start --browsers Firefox --single-run"
  },
  "devDependencies": {
    "jasmine-core": "^2.5.2",
    "karma": "^1.3.0",
    "karma-chrome-launcher": "^2.0.0",
    "karma-firefox-launcher": "^1.0.0"
  }
}
