
describe('CanvasSize', function()

{

  var width = window.innerWidth;
  var height = window.innerHeight;

  it("Should equal the size of our window", function() {
    expect(width).toBe(true);
    expect(height).toBe(true);

});

});
